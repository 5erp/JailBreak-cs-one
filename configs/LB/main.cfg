lb_sql_host 	"46.174.50.251"	// Хост SQL
lb_sql_user 	"admin_jbuser"		// Юзер SQL
lb_sql_pass 	""		// Пасс SQL
lb_sql_db 		"admin_jbserver"		// Имя БД SQL
lb_sql_pref 	"amx"		// Префикс таблиц SQL

lb_server_ip 	"37.230.210.119:27015" 	// Адрес сервера
lb_server_name 	"Маленькая тюрьма"		// Имя сервера

lb_kick_delay 	"3"	// Задержка перед киком (актуально для скриншотеров).
lb_all_bans 	"0"	// Общий бан-лист на все сервера, подключенные к БД

lb_static_reason 	"1"	// Меню причин (см. файл reasons.ini)
lb_static_bantime 	"0"	// [Только при lb_static_reason 1] Для каждой причины фиксированное время бана (см. файл reasons.ini)
lb_custom_reason 	"1"	// [Только при lb_static_reason 1] Своя причина для бана + время из файла times.ini
lb_remove_expired 	"0"	// Удалять истекшие баны из БД. Если 0, то выставляет expired 1 (в бан листе строка становится зеленой)
lb_link_to_banphp 	""	// Ссылка вида http://site.ru/ban.php (если не указать ссылку, отвалится cookie бан)

lb_hud_text "Игрок %player% забанен!%n%Продолжительность: %banlen%%n%Администратор: %admin%%n%Причина: %reason%"	
	// HUD сообщение о бане игрока.
	// Ключи для автозамены
	// %n% 		- новая строка
	// %player% 	- ник игрока
	// %admin% 	- ник админа
	// %banlen% 	- продолжительность бана
	// %reason% 	- причина бана
lb_hud_pos "0.6 0.04" // Координаты HUD сообщения о бане игрока
lb_hud_color "0 255 0" // Цвет HUD сообщения в формате R G B